import { updateUser } from '../utils/database.js';
import { parseAmount, formatMoney } from '../utils/formatting.js';
import { notice } from '../utils/ui.js';
export default { name: 'removebal', aliases: [], adminOnly: true, async execute(message, args) { const target = message.mentions.users.first(); const amount = parseAmount(args.find((a) => /^\d/.test(a))); if (!target) return message.reply(notice('❌ Missing User', 'Mention a user to debit.')); if (!amount) return message.reply(notice('❌ Invalid Amount', 'Enter a positive number with up to two decimals.')); let ok = false; const user = await updateUser(target.id, (u) => { if (u.withdrawable >= amount) { u.withdrawable -= amount; ok = true; } }); if (!ok) return message.reply(notice('❌ Insufficient Balance', 'Insufficient withdrawable balance.')); await message.reply(notice('✅ Balance Removed', `**User**\n${target}\n\n**Removed**\n${formatMoney(amount)}\n\n**Withdrawable**\n${formatMoney(user.withdrawable)}\n\n**Total**\n${formatMoney(user.total)}`)); } };
